
let firstName = 'Majedul';
let lastName = 'Hasan';

let fullName = `${firstName} ${lastName} `;
console.log(fullName)


let serchResults=0
let outPut = `${serchResults>0 ? `${serchResults} results` : 'no serch results'} `

console.log(outPut)

